/**
 * Hero 3D model: place `.glb` files in `public/models/` (served as `/models/...`).
 *
 * Current asset: `office-pc.glb` (from your office PC export).
 * Optional FBX fallback: `public/models/custom-gaming-pc.fbx`
 *
 * Load order: GLB first, then FBX (see HeroPCModel).
 */
export const HERO_PC_MODEL_PATH_GLB = "/models/office-pc.glb";
export const HERO_PC_MODEL_PATH_FBX = "/models/custom-gaming-pc.fbx";
